# きみのまなび おてつだい LP

小中学生専門オンライン学習塾「きみのまなび おてつだい」のランディングページ。
単一HTML（`index.html`）＋ 画像（`assets/`）の静的サイト。

## 保存＆公開

```
git add -A
git commit -m "更新内容"
git push
```

`git push` するだけで GitHub に保存され、GitHub Pages が自動で再公開する。

- 公開URL: https://sekitaeisuke.github.io/kiminomanabi/
- ホスティング: GitHub Pages（main ブランチ / ルート）
- 更新は `index.html` を編集して push するだけ。

## 編集メモ

- お問い合わせフォーム（`#contact`）は現在デザイン見本。実フォーム（STUDIO等）に差し替え予定。
- LINE: https://lin.ee/XR2idcG ／ 電話: 0120-91-6374
